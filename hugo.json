{
  "baseURL": "https://tsvsheet.github.io/docs.tsvsheet.go/",
  "title": "tsvsheet",
  "publishDir": "_site",
  "enableRobotsTXT": true,
  "markup": {
    "goldmark": {
      "renderHooks": {
        "link": { "useEmbedded": "fallback" },
        "image": { "useEmbedded": "fallback" }
      }
    }
  },
  "params": {
    "description": "A spreadsheet for plain text — a .tsvt file is the spreadsheet."
  }
}
